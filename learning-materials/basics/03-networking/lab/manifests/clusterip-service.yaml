# TODO: Create a ClusterIP Service
# HINT: Select pods with app: api label
# HINT: Port 80 -> targetPort 8080
apiVersion: v1
kind: Service
metadata:
  name: api
spec:
  type: ClusterIP
  # TODO: Add selector
  ports:
  - port: 80
    # TODO: Add targetPort
