# TODO: Create NetworkPolicy (deny-all, then allow specific)
# HINT: Start with podSelector: {} for all pods
# HINT: Set policyTypes: [Ingress, Egress]
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-all
  namespace: production
spec:
  # TODO: Configure default deny policy
